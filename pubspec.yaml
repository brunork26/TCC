name: tcc
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  flutter_tts: ^1.3.0
  flutter_image_map: ^0.9.2
  expanded_grid: ^0.0.2
  reorderables:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - imagens/
    - assets/
    - assets/cards/
    - assets/cards/pessoas/
    - assets/cards/pronomes/